import Component from '../../components/otp/src/otp.component.js';
/**
 * @summary OTP captures one-time passcodes with a single accessible input and segmented visual slots.
 * @documentation '../stories/otp.mdx'
 *
 * @fires mde-complete {CustomEvent} - Emitted when the entered code reaches the configured length.
 * @fires change {Event} - Fired when the user commits a value change.
 *
 * @fires input {InputEvent} - Fired whenever the value changes from user input.
 * @fires invalid {Event} - Fired when the control has been checked for validity and does not satisfy its constraints.
 *
 * @slot label - The otp field's label. Alternatively, use the `label` attribute.
 * @slot supporting-text - Optional instructional or validation text below the label.
 *
 * @method focus(options?: FocusOptions) - Moves focus to the internal `<input>` element.
 * @method blur() - Removes focus from the internal `<input>` element.
 * @method checkValidity() - Returns `true` if the control satisfies its validation constraints.
 * @method reportValidity() - Checks validity and, if invalid, shows a validation message. Returns `true` if valid.
 * @method setCustomValidity(message: string) - Sets a custom validation message. Pass an empty string to clear.
 *
 * @csspart label - The `<label>` element.
 * @csspart input-container - The wrapper that contains the visual slots and hidden input.
 * @csspart slot - Each visual OTP slot.
 * @csspart slot-character - The visible character inside each slot.
 * @csspart input - The internal `<input>` element.
 * @csspart validation-text - The validation message element.
 */
declare const reactWrapper: import("../create-component.js").ReactWebComponent<Component, {
    onMdeComplete: string;
    onChange: string;
}>;
export default reactWrapper;
//# sourceMappingURL=index.d.ts.map