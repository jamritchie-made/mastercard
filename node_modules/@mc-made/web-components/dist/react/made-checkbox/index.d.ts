import Component from '../../components/checkbox/src/checkbox.component.js';
/**
 * @summary Checkboxes are used when there are multiple items to select in a list. Users can select zero, one, or any number of items.
 * @documentation '../../components/checkbox/stories/checkbox.mdx'
 *
 * @dependency made-icon
 *
 * @fires change {Event} - Fired when an alteration to the control's value is committed by the user.
 * @fires input {Event} - Fired when the control receives input.
 * @fires invalid {Event} - Fired when the control has been checked for validity and doesn't satisfy its constraints.
 *
 * @slot label - The checkbox's label. Alternatively, you can use the `label` attribute.
 * @slot help-text - Text that describes how to use the checkbox. Alternatively, you can use the `help-text` attribute.
 *
 * @csspart base - The component's base wrapper.
 * @csspart control - Container that wraps the control.
 * @csspart checked-icon - The checked icon element.
 * @csspart indeterminate-icon - The indeterminate icon element.
 * @csspart label - The label's wrapper.
 * @csspart supporting-text - The help text's wrapper.
 */
declare const reactWrapper: import("../create-component.js").ReactWebComponent<Component, {
    onChange: string;
}>;
export default reactWrapper;
//# sourceMappingURL=index.d.ts.map