import * as React from 'react';
import { createComponent } from '../create-component.js';
import Component from '../../components/card/src/card-content.component.js';
const tagName = 'made-card-content';
Component.define(tagName);
/**
 * @summary Cards can be used to group related information into flexible containers.
 * @documentation TODO
 *
 * @slot - The card's content.
 *
 * @csspart base - The component's base wrapper.
 *
 */
const reactWrapper = createComponent({
    tagName,
    elementClass: Component,
    react: React,
    events: {},
    displayName: 'MadeCardContent'
});
export default reactWrapper;
