import MadeElement from '../../../internal/made-element.js';
import MadeLink from '../../link/made-link.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary A breadcrumb item represents a single link within a breadcrumb navigation trail.
 *
 * @dependency made-link
 *
 * @slot - The breadcrumb item's content. When provided, it will be used instead of the built-in made-link.
 *
 * @csspart base - The component's base wrapper (wraps the made-link or slotted content).
 * @csspart link - The underlying made-link element (only when using prop-based API).
 * @csspart link-base - The base part of the made-link (only when using prop-based API).
 * @csspart link-label - The label part of the made-link (only when using prop-based API).
 * @csspart current - The wrapper for the current page item (only when current is true and using prop-based API).
 */
export default class MadeBreadcrumbItem extends MadeElement {
    static styles: CSSResultGroup;
    static dependencies: {
        'made-link': typeof MadeLink;
    };
    /** URL that the breadcrumb item points to. */
    href?: string;
    /** Tells the browser where to open the link. */
    target?: '_blank' | '_parent' | '_self' | '_top';
    /** The relationship of the linked URL as space-separated link types. */
    rel?: string;
    /**
     * Marks this breadcrumb item as the current/active page in the breadcrumb trail.
     * When true, the item will be styled as active and aria-current will be set to "page".
     *
     * @internal Set by `made-breadcrumb` — not part of the public API.
     */
    protected current: boolean;
    /** Disables the link and styles it as inactive. Only applicable when using the build-in made-link */
    disabled: boolean;
    /** An accessible label that describes the breadcrumb item. */
    ariaLabel: string | null;
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=breadcrumb-item.component.d.ts.map