import MadeElement from '../../../internal/made-element.js';
import MadeIcon from '../../icon/made-icon.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary A visual representation of a user or entity.
 * @documentation '../stories/avatar.mdx'
 *
 * @dependency made-icon
 *
 * @csspart base - The component's base wrapper.
 * @csspart icon - The icons wrapper.
 * @csspart image - The avatar image.
 * @csspart initials - The container that wraps the avatar's initials.
 *
 */
export type Size = 'x-small' | 'small' | 'medium' | 'large' | 'x-large' | 'xx-large';
export default class MadeAvatar extends MadeElement {
    static styles: CSSResultGroup;
    static dependencies: {
        'made-icon': typeof MadeIcon;
    };
    private hasError;
    /** Shape of the avatar. */
    shape: 'circle' | 'squared';
    /** Size of the avatar. */
    size: Size;
    /** Initials to use as a fallback when no image is available (1-2 characters max recommended). */
    initials: string;
    /** Source of the avatar's image. */
    image: string;
    /** Use this to describe the avatar for assistive technologies. */
    alt: string;
    /** Instruct the browser how to load the image. */
    loading: 'eager' | 'lazy';
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=avatar.component.d.ts.map