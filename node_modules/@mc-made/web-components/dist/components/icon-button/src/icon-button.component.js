/* v8 ignore next 6 */
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { classMap } from 'lit/directives/class-map.js';
import { nothing } from 'lit';
import { html, literal } from 'lit/static-html.js';
import { ifDefined } from 'lit/directives/if-defined.js';
import { property, query } from 'lit/decorators.js';
import componentStyles from '../../../styles/component.styles.js';
import MadeElement from '../../../internal/made-element.js';
import MadeIcon from '../../icon/src/icon.component.js';
import styles from './icon-button.styles.js';
/**
 * @summary Use icon buttons to display actions in a compact layout.
 * Icon buttons can represent opening actions such as opening an
 * overflow menu or search, or represent binary actions that can be
 * toggled on and off, such as favorite or bookmark.
 * @documentation '../stories/icon-button.mdx'
 *
 * @fires input {InputEvent} Dispatched when a toggle button toggles
 * @fires change {Event} Dispatched when a toggle button toggles
 *
 * @dependency made-icon
 *
 * @slot - The icon button's content.
 *
 * @csspart base - The component's base wrapper.
 * @csspart icon - The component's icon wrapper.
 * @csspart icon-selected - The component's icon wrapper when selected.
 */
class MadeIconButton extends MadeElement {
    constructor() {
        super(...arguments);
        /** Size of the button. */
        this.size = 'medium';
        /** The button's variant. */
        this.variant = 'primary';
        /**
         * When true, the button will toggle between selected and unselected
         * states
         */
        this.toggle = false;
        /**
         * Sets the selected state. When false, displays the default icon. When true,
         * displays 'icon-selected', or the default icon if 'icon-selected' is not provided.
         */
        this.selected = false;
        /** Disables the button and makes it non-interactive. */
        this.disabled = false;
    }
    /** The name of the button. */
    get name() {
        return this.getAttribute('name') ?? '';
    }
    set name(name) {
        this.setAttribute('name', name);
    }
    async handleClick(event) {
        await 0; // eslint-disable-line
        if (this.disabled) {
            event.preventDefault();
            event.stopPropagation();
            return;
        }
        if (this.toggle) {
            this.selected = !this.selected;
            this.dispatchEvent(new InputEvent('input', { bubbles: true, composed: true }));
            this.dispatchEvent(new Event('change', { bubbles: true }));
        }
    }
    /** Simulates a click on the icon button. */
    click() {
        this.button.click();
    }
    /** Sets focus on the icon button. */
    focus(options) {
        this.button.focus(options);
    }
    /** Removes focus from the icon button. */
    blur() {
        this.button.blur();
    }
    render() {
        const isLink = this.href ? true : false;
        const tag = isLink ? literal `a` : literal `button`;
        const ariaPressedValue = !this.toggle ? nothing : this.selected;
        const ariaLabelValue = this.ariaLabelSelected && this.selected
            ? this.ariaLabelSelected
            : this.ariaLabel;
        const hasSelectedIcon = Boolean(this.iconNameSelected || this.iconSrcSelected);
        /* eslint-disable lit/binding-positions, lit/no-invalid-html */
        return html `
      <${tag}
        part="base"
        class=${classMap({
            'made-c-icon-button': true,
            'made-c-icon-button--primary': this.variant === 'primary',
            'made-c-icon-button--secondary': this.variant === 'secondary',
            'made-c-icon-button--destructive': this.variant === 'destructive',
            'made-c-icon-button--ghost': this.variant === 'ghost',
            'made-c-icon-button--small': this.size === 'small',
            'made-c-icon-button--medium': this.size === 'medium',
            'made-c-icon-button--large': this.size === 'large',
            'made-c-icon-button--disabled': !isLink && this.disabled,
            'made-c-icon-button--selected': this.toggle && this.selected,
            'made-c-icon-button--has-icon-selected': hasSelectedIcon
        })}
        ?disabled=${ifDefined(isLink ? undefined : this.disabled)}
        type=${ifDefined(isLink ? undefined : 'button')}
        href=${ifDefined(isLink ? this.href : undefined)}
        target=${ifDefined(isLink ? this.target : undefined)}
        download=${ifDefined(isLink ? this.download : undefined)}
        rel=${ifDefined(isLink && this.target ? 'noreferrer noopener' : undefined)}
        role=${ifDefined(isLink ? undefined : 'button')}
        aria-disabled=${this.disabled ? 'true' : 'false'}
        aria-label=${ariaLabelValue || nothing}
        aria-pressed=${ariaPressedValue}
        tabindex=${this.disabled ? '-1' : '0'}
        @click=${!isLink ? this.handleClick : undefined}
      >
      <slot>
        <span class="made-c-icon-button__icon-container">
          <made-icon
            part="icon-selected"
            class="made-c-icon-button__icon-selected"
            name=${ifDefined(this.iconNameSelected)}
            library=${ifDefined(this.library)}
            src=${ifDefined(this.iconSrcSelected)}
            size="medium"
            aria-hidden="true"
          ></made-icon>
          <made-icon
            part="icon"
            class="made-c-icon-button__icon"
            name=${ifDefined(this.iconName)}
            library=${ifDefined(this.library)}
            src=${ifDefined(this.iconSrc)}
            size="medium"
            aria-hidden="true"
          ></made-icon>
        </span>
      </slot>
      </${tag}>
    `;
    }
}
MadeIconButton.styles = [componentStyles, styles];
MadeIconButton.dependencies = { 'made-icon': MadeIcon };
export default MadeIconButton;
__decorate([
    query('.made-c-icon-button')
], MadeIconButton.prototype, "button", void 0);
__decorate([
    property({ attribute: 'icon-name' })
], MadeIconButton.prototype, "iconName", void 0);
__decorate([
    property({ attribute: 'icon-name-selected' })
], MadeIconButton.prototype, "iconNameSelected", void 0);
__decorate([
    property()
], MadeIconButton.prototype, "library", void 0);
__decorate([
    property()
], MadeIconButton.prototype, "size", void 0);
__decorate([
    property({ reflect: true })
], MadeIconButton.prototype, "variant", void 0);
__decorate([
    property({ attribute: 'icon-src' })
], MadeIconButton.prototype, "iconSrc", void 0);
__decorate([
    property({ attribute: 'icon-src-selected' })
], MadeIconButton.prototype, "iconSrcSelected", void 0);
__decorate([
    property()
], MadeIconButton.prototype, "href", void 0);
__decorate([
    property()
], MadeIconButton.prototype, "target", void 0);
__decorate([
    property({ type: Boolean })
], MadeIconButton.prototype, "toggle", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MadeIconButton.prototype, "selected", void 0);
__decorate([
    property({ reflect: true })
], MadeIconButton.prototype, "value", void 0);
__decorate([
    property()
], MadeIconButton.prototype, "download", void 0);
__decorate([
    property({ attribute: 'aria-label' })
], MadeIconButton.prototype, "ariaLabel", void 0);
__decorate([
    property({ attribute: 'aria-label-selected' })
], MadeIconButton.prototype, "ariaLabelSelected", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MadeIconButton.prototype, "disabled", void 0);
