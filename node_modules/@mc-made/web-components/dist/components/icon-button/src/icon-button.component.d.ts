import { type CSSResultGroup } from 'lit';
import MadeElement from '../../../internal/made-element.js';
import MadeIcon from '../../icon/src/icon.component.js';
/**
 * @summary Use icon buttons to display actions in a compact layout.
 * Icon buttons can represent opening actions such as opening an
 * overflow menu or search, or represent binary actions that can be
 * toggled on and off, such as favorite or bookmark.
 * @documentation '../stories/icon-button.mdx'
 *
 * @fires input {InputEvent} Dispatched when a toggle button toggles
 * @fires change {Event} Dispatched when a toggle button toggles
 *
 * @dependency made-icon
 *
 * @slot - The icon button's content.
 *
 * @csspart base - The component's base wrapper.
 * @csspart icon - The component's icon wrapper.
 * @csspart icon-selected - The component's icon wrapper when selected.
 */
export default class MadeIconButton extends MadeElement {
    static styles: CSSResultGroup;
    static dependencies: {
        'made-icon': typeof MadeIcon;
    };
    button: HTMLButtonElement | HTMLLinkElement;
    /** The name of the icon to draw. Available names depend on the icon library being used. */
    iconName: string;
    /** The name of the icon to draw when button is toggleable and selected.
     * Fallback is `icon` if not set.
     */
    iconNameSelected: string;
    /** The name of a registered custom icon library. */
    library?: string;
    /** Size of the button. */
    size: 'small' | 'medium' | 'large';
    /** The button's variant. */
    variant: 'primary' | 'secondary' | 'destructive' | 'ghost';
    /**
     * An external URL of an SVG file. Be sure you trust the content to avoid XSS attacks.
     */
    iconSrc?: string;
    /**
     * An external URL of an SVG file to draw  when button is toggleable and selected.
     * Fallback is `src` if not set.
     */
    iconSrcSelected?: string;
    /** When set, the underlying button will be rendered as an `<a>` with this `href` instead of a `<button>`. */
    href?: string;
    /** Tells the browser where to open the link. Only used when `href` is set. */
    target?: '_blank' | '_parent' | '_self' | '_top';
    /**
     * When true, the button will toggle between selected and unselected
     * states
     */
    toggle: boolean;
    /**
     * Sets the selected state. When false, displays the default icon. When true,
     * displays 'icon-selected', or the default icon if 'icon-selected' is not provided.
     */
    selected: boolean;
    /**
     * Associate a value with the button.
     */
    value: string;
    /** The name of the button. */
    get name(): string;
    set name(name: string);
    /**
     * The filename to use when downloading the linked resource.
     * If not specified, the browser will determine a filename.
     * This is only applicable when the icon button is used as a link (`href` is set).
     *
     */
    download?: string;
    /**
     * A description that gets read by assistive devices. For optimal accessibility,
     * you should always include a label that describes what the icon button does.
     */
    ariaLabel: string;
    /**
     * Add an aria-label-selected attribute to toggle buttons whose labels need a
     * more descriptive label when selected.
     */
    ariaLabelSelected: string;
    /** Disables the button and makes it non-interactive. */
    disabled: boolean;
    private handleClick;
    /** Simulates a click on the icon button. */
    click(): void;
    /** Sets focus on the icon button. */
    focus(options?: FocusOptions): void;
    /** Removes focus from the icon button. */
    blur(): void;
    render(): import("lit-html").TemplateResult;
}
//# sourceMappingURL=icon-button.component.d.ts.map