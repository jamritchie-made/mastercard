import { css } from 'lit';
export default css `
  :host {
    --width: 1em;
    --height: 1em;

    display: flex;
    width: var(--width);
    height: var(--height);
    box-sizing: content-box !important;
  }

  /*
   * Size modifiers
   */

  :host([size='small']) {
    font-size: 1rem;
  }

  :host([size='medium']) {
    font-size: 1.25rem;
  }

  :host([size='large']) {
    font-size: 1.5rem;
  }

  svg {
    display: block;
    height: 100%;
    width: 100%;
  }
`;
