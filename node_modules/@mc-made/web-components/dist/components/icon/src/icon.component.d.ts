import MadeElement from '../../../internal/made-element.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary Icons are symbols that can be used to represent various options within an application.
 * @documentation '../stories/icon.mdx'
 *
 * @fires mde-load {CustomEvent} - Fired when the icon has loaded.
 * @fires mde-error {CustomEvent} - Fired when the icon fails to load due to an error.
 *
 * @csspart base - The component's base wrapper.
 * @csspart svg - The internal SVG element.
 */
export default class MadeIcon extends MadeElement {
    static styles: CSSResultGroup;
    private svg;
    /** The name of the system icon to draw. */
    name?: string;
    /** URL of an SVG file. */
    src?: string;
    /** The name of icon library. */
    library: string;
    /**
     * An alternate description to use for assistive devices. If omitted, the icon will be considered presentational and
     * ignored by assistive devices.
     */
    label: string;
    /** Default icon size is 1em which is great for inline icons. Use this if you need more control over the icon's size */
    size?: 'small' | 'medium' | 'large';
    /** Given a URL, this function returns the resulting SVG element or an appropriate error symbol. */
    private resolveIcon;
    connectedCallback(): void;
    firstUpdated(): void;
    disconnectedCallback(): void;
    private getIconSource;
    updated(changedProperties: Map<string | number | symbol, unknown>): void;
    handleLabelChange(): void;
    setIcon(): Promise<void>;
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=icon.component.d.ts.map