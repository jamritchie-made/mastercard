import { getBasePath } from '../../utilities/base-path.js';
const defaultLibrary = {
    name: 'default',
    resolver: name => getBasePath(`assets/icons/system-icons/${name}.svg`)
};
export default defaultLibrary;
