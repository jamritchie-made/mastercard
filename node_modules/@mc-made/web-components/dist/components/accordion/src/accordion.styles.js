import { css } from 'lit';
export default css `
  :host {
    display: block;
  }

  .made-c-accordion-container {
    display: flex;
    flex-direction: column;
    gap: var(--made-size-space-2-x);
  }
`;
