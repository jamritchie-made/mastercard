import MadeAccordionItem from '../../accordion-item/src/accordion-item.component.js';
import MadeElement from '../../../internal/made-element.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary An accordion is a vertically stacked list of headers that reveal or hide associated sections of content.
 * @documentation '../stories/accordion.mdx'
 *
 * @dependency made-accordion-item
 *
 * @fire mde-show - Emitted when an accordion item is expanded.
 * @fire mde-hide - Emitted when an accordion item is collapsed.
 *
 * @slot - One or more made-accordion-item elements.
 *
 * @csspart base - The component's base wrapper.
 */
export default class MadeAccordion extends MadeElement {
    static styles: CSSResultGroup;
    static dependencies: {
        'made-accordion-item': typeof MadeAccordionItem;
    };
    private items;
    /** A convenience property to set the appearance style of each accordion item. */
    appearance: 'default' | 'contained';
    /** When true, only one accordion item can be expanded at a time. */
    exclusive: boolean;
    /** A convenience property to disable all accordion items. */
    disabled: boolean;
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(): void;
    updated(changedProperties: Map<string, unknown>): void;
    /** Expands all accordion items (only works when exclusive is false). */
    expandAll(): void;
    /** Collapses all accordion items. */
    collapseAll(): void;
    private syncItemStates;
    private handleItemToggle;
    private handleKeyDown;
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=accordion.component.d.ts.map