import { createValidator, getValidityAnchor } from '../../../internal/face/constraint-validation.js';
import { getFormValue } from '../../../internal/face/form-associated.js';
import { onReportValidity } from '../../../internal/face/on-report-validity.js';
import MadeElement from '../../../internal/made-element.js';
import MadeIcon from '../../icon/src/icon.component.js';
import MadeIconButton from '../../icon-button/made-icon-button.js';
import type { CSSResultGroup } from 'lit';
import type { Validator } from '../../../internal/face/validators/validator.js';
/**
 * @summary Inputs collect data from the user.
 * @documentation '../stories/input.mdx'
 *
 * @dependency made-icon
 *
 * @slot label - The input's label. Alternatively, you can use the `label` attribute.
 * @slot start - Optional slot used to place an icon or prefix at the start of the input.
 * @slot end - Optional slot used to place an icon or suffix at the end of the input.
 * @slot clear-icon - An icon to use in lieu of the default clear icon.
 * @slot show-password-icon - An icon to use in lieu of the default show password icon.
 * @slot hide-password-icon - An icon to use in lieu of the default hide password icon.
 *
 * @fires change {Event} - Fired when an alteration to the control's value is committed by the user.
 * @fires input {Event} - Fired when the control receives input.
 * @fires invalid {Event} - Fired when the control has been checked for validity and doesn't satisfy its constraints.
 * @fires mde-clear {CustomEvent} - Fired when the clear button is activated.
 *
 * @csspart form-control - The outer wrapper.
 * @csspart label - The label's wrapper.
 * @csspart character-count - The character-count's wrapper.r.
 * @csspart supporting-text - The help text's wrapper.
 * @csspart input-container - The container that wraps the input and any associated elements.
 * @csspart input - The internal `<input>` control.
 * @csspart start - The container that wraps the start slot.
 * @csspart clear-button - The clear button.
 * @csspart password-toggle-button - The password toggle button.
 * @csspart end - The container that wraps the end slot.
 */
declare const inputBaseClass: import("../../../internal/face/mixin.js").MixinReturn<import("../../../internal/face/mixin.js").MixinReturn<import("../../../internal/face/mixin.js").MixinReturn<(abstract new (...args: any[]) => import("../../../internal/face/element-internals.js").WithElementInternals) & typeof MadeElement & import("../../../internal/face/form-associated.js").FormAssociatedConstructor, import("../../../internal/face/form-associated.js").FormAssociated>, import("../../../internal/face/constraint-validation.js").ConstraintValidation>, import("../../../internal/face/on-report-validity.js").OnReportValidity>;
export default class MadeInput extends inputBaseClass {
    static styles: CSSResultGroup;
    static dependencies: {
        'made-icon': typeof MadeIcon;
        'made-icon-button': typeof MadeIconButton;
    };
    private LABEL;
    private readonly hasSlotController;
    private isSafari;
    /** Disables the input. */
    disabled: boolean;
    /** The name of the input, submitted as a name/value pair with form data. */
    name: string;
    private input;
    private hasFocus;
    private passwordVisible;
    /**
     * Whether or not a native error has been reported via `reportValidity()`.
     */
    private hasNativeError;
    /**
     * The validation message displayed from a native error via
     * `reportValidity()`.
     */
    private nativeErrorText;
    private get hasError();
    title: string;
    private __numberInput;
    private __dateInput;
    /**
     * Gets or sets whether or not the text field is in a visually invalid state.
     *
     * This error state overrides the error state controlled by
     * `reportValidity()`.
     */
    error: boolean;
    /**
     * The error message that replaces supporting text when `error` is true. If
     * `errorText` is an empty string, then the supporting text will continue to
     * show.
     *
     * This error message overrides the error message displayed by
     * `reportValidity()`.
     */
    errorText: string;
    /**
     * Gets or sets an explicit visually valid state. Can be used as part of
     * a manual vaidation process.
     *
     * The error state returned by `reportValidity()` will override this.
     */
    success: boolean;
    /**
     * The success message that replaces supporting text when `success` is true.
     * If `errorText` is an empty string, then the supporting text will continue to
     * show.
     *
     * The message returned by `reportValidity()` will override this.
     */
    successText: string;
    /**
     * The type of input. Works the same as a native `<input>` element, but only a subset of types are supported. Defaults
     * to `text`.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#input_types
     */
    type: 'date' | 'datetime-local' | 'email' | 'number' | 'password' | 'search' | 'tel' | 'text' | 'time' | 'url';
    /** The current value of the input, submitted as a name/value pair with form data. */
    value: string;
    /** The input's label. If you need to display HTML, use the `label` slot instead. */
    label: string;
    /** The input's size. */
    size: 'medium' | 'large';
    /**
     * Provides extra details below the text field, including instructions on its usage.
     */
    supportingText: string;
    /** Adds a clear button when the input is not empty. */
    clearable: boolean;
    /**
     * Placeholder text to show as a hint when the input is empty.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/placeholder
     */
    placeholder: string;
    /**
     * Makes the input readonly.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#readonly
     */
    readonly: boolean;
    /** Adds a button to toggle the password's visibility. Only applies to password types. */
    passwordToggle: boolean;
    /** Hides the browser's built-in increment/decrement spin buttons for number inputs. */
    noSpinner: boolean;
    /**
     * Makes the input a required field.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/required
     */
    required: boolean;
    /** Visual indicators that are appended to the label of a required field. */
    requiredIndicator: 'required' | '*';
    /** Add the optional visual indicator to a non-required field. */
    optional: boolean;
    /** Hides the required visual indicator on the label, when the text field is required. */
    noRequiredIndicator: boolean;
    /**
     * A regular expression pattern to validate input against.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#pattern
     */
    pattern: string;
    /**
     * The minimum length of input that will be considered valid.
     * Set to -1 for none.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#minlength
     */
    minlength: number;
    /**
     * The maximum length of input that will be considered valid.
     * Set to -1 for none.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#maxlength
     */
    maxlength: number;
    /**
     * The input's minimum value. Only applies to date and number input types.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#min
     */
    min: number | string;
    /**
     * The input's maximum value. Only applies to date and number input types.
     *
     *  https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#max
     */
    max: number | string;
    /** Shows the character count if input type is 'text' and maxlength is set. */
    characterCount: boolean;
    /**
     * Returns or sets the element's step attribute, which works with min and max
     * to limit the increments at which a numeric or date-time value can be set.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#step
     */
    step: number | 'any';
    /** Controls whether and how text input is automatically capitalized as it is entered by the user on a virtual keyboard. */
    autocapitalize: 'off' | 'none' | 'on' | 'sentences' | 'words' | 'characters';
    /**
     * Describes what, if any, type of autocomplete functionality the input
     * should provide.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete
     */
    autocomplete: string;
    /**
     * Indicates that the input should receive focus on page load.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/autofocus
     * */
    autofocus: boolean;
    /**
     * Used to customize the label or icon of the Enter key on virtual keyboards.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/enterkeyhint
     * */
    enterkeyhint: 'enter' | 'done' | 'go' | 'next' | 'previous' | 'search' | 'send';
    /**
     * Tells the browser what type of data will be entered by the user, allowing it to display the appropriate virtual
     * keyboard.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/inputmode
     */
    inputmode: 'none' | 'text' | 'decimal' | 'numeric' | 'tel' | 'search' | 'email' | 'url';
    /**
     * Gets or sets the current value as a `Date` object. Returns `null` if the value can't be converted. This will use the native `<input type="{{type}}">` implementation and may result in an error.
     */
    get valueAsDate(): Date | null;
    set valueAsDate(newValue: Date | null);
    /** Gets or sets the current value as a number. Returns `NaN` if the value can't be converted. */
    get valueAsNumber(): number;
    set valueAsNumber(newValue: number);
    updated(changedProperties: Map<string | number | symbol, unknown>): void;
    private convertTodayToDate;
    private handleBlur;
    private handleChange;
    private handleClearClick;
    private handleFocus;
    private handleInput;
    private handlePasswordToggle;
    /** Prevent toggle from stealing focus */
    private handlePointerDown;
    /** Sets focus on the input. */
    focus(options?: FocusOptions): void;
    /** Removes focus from the input. */
    blur(): void;
    /** Selects all the text in the input.
     *
     * https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/select
     */
    select(): void;
    /**
     * Sets the start and end positions of the text selection (0-based).
     *
     * https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/setSelectionRange
     */
    setSelectionRange(selectionStart: number, selectionEnd: number, selectionDirection?: 'forward' | 'backward' | 'none'): void;
    /**
     * Replaces a range of text with a new string.
     *
     * https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/setRangeText
     */
    setRangeText(replacement: string, start?: number, end?: number, selectMode?: 'select' | 'start' | 'end' | 'preserve'): void;
    /**
     * Displays the browser picker for an input element (only works if the browser supports it for the input type).
     *
     * https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/showPicker
     * */
    showPicker(): void;
    /** Increments the value of a numeric input type by the value of the step attribute.
     *
     * https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/stepUp
     */
    stepUp(): void;
    /**
     * Decrements the value of a numeric input type by the value of the step attribute.
     *
     * https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/stepDown
     */
    stepDown(): void;
    private handleKeyDown;
    private get errorOrSuccessText();
    private get supportingOrValidationText();
    private renderSupportingText;
    render(): import("lit-html").TemplateResult<1>;
    /**
     * Reset the text field to its default value.
     */
    private reset;
    private getErrorText;
    [getFormValue](): string;
    formResetCallback(): void;
    formStateRestoreCallback(state: string): void;
    [createValidator](): Validator<unknown>;
    [getValidityAnchor](): HTMLElement | null;
    [onReportValidity](invalidEvent: Event | null): void;
}
export {};
//# sourceMappingURL=input.component.d.ts.map