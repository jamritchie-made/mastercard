export type BuiltInThemeName = 'unify' | 'mastercard';
/** Built-in theme names plus any consumer-registered custom theme name. */
export type ThemeName = BuiltInThemeName | (string & {});
export type ThemeColorMode = 'light' | 'dark' | 'auto';
export type ThemeType = Map<ThemeColorMode, CSSStyleSheet>;
export type ThemeMap = Map<ThemeName, ThemeType>;
/** CSS custom property key/value pairs used to define a theme via {@link MadeThemeProvider.createTheme}. */
export type ThemeProperties = Record<string, string>;
/** Concrete colour modes — excludes the `'auto'` resolution hint. */
export type ThemeColorModeActual = Exclude<ThemeColorMode, 'auto'>;
/**
 * Options accepted by {@link MadeThemeProvider.createTheme}.
 */
export interface CreateThemeOptions {
    name: string;
    /**
     * Optionally base this theme on a built-in theme. The built-in theme's
     * stylesheet is applied first, and only the properties supplied in `modes`
     * are overridden.
     */
    basedOn?: BuiltInThemeName;
    /**
     * A CSS string of `@font-face` declarations to load alongside this theme.
     * The string is injected into `document.adoptedStyleSheets` exactly once —
     * subsequent calls to `createTheme` with the same `name` will not re-inject
     * the fonts.
     */
    fonts?: string;
    modes: Partial<Record<ThemeColorModeActual, ThemeProperties>>;
}
//# sourceMappingURL=theme-provider-interfaces.d.ts.map