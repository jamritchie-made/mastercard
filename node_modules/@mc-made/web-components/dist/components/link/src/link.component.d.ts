import MadeElement from '../../../internal/made-element.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary A link allow users to navigate to a different location. They can be presented in-line inside a paragraph or as standalone text.
 * @documentation '../stories/link.mdx'
 *
 * @slot - The link's label.
 * @slot start - Used to place an content before the link's label. Typically used for icons.
 * @slot end - Used to place content after the link's label. Typically used for icons.
 *
 * @csspart base - The component's base wrapper.
 * @csspart end - The container that wraps the end slot.
 * @csspart label - The link's label.
 * @csspart start - The container that wraps the start slot.
 *
 */
export default class MadeLink extends MadeElement {
    static styles: CSSResultGroup;
    private readonly hasSlotController;
    link: HTMLLinkElement;
    private hasFocus;
    /** Alternative ways to underline link. */
    decoration: 'underline' | 'subtle';
    /** Use the appearance attribute to change the visual style of the link. */
    appearance: 'neutral' | 'branded';
    /** URL that the hyperlink points to. */
    href: string;
    /** An accessible label that describes the component. */
    ariaLabel: string;
    /** Setting true disabled the link. */
    disabled: boolean;
    /** Download a resource as opposed to navigating to it. */
    download?: string | true;
    /** Tells the browser where to open the link. */
    target?: '_blank' | '_parent' | '_self' | '_top';
    /** The relationship of the linked URL as space-separated link types. */
    rel?: string;
    /** How much of the referrer to send when following the link. */
    referrerpolicy?: 'no-referrer' | 'no-referrer-when-downgrade' | 'origin' | 'origin-when-cross-origin' | 'same-origin' | 'strict-origin' | 'strict-origin-when-cross-origin' | 'unsafe-url';
    private handleBlur;
    private handleFocus;
    /** Simulates a click on the link. */
    click(): void;
    /** Sets focus on the link. */
    focus(options?: object): void;
    /** Removes focus from the link. */
    blur(): void;
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=link.component.d.ts.map