import { type FormSubmitter } from '../../../internal/face/form-submitter.js';
import MadeElement from '../../../internal/made-element.js';
import MadeLoader from '../../loader/made-loader.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary Buttons represent actions that are available to the user.
 * @documentation '../stories/button.mdx'
 *
 * @slot - The button's label.
 * @slot start - Used to place content before button text. Typically used for icons.
 * @slot end - Used to place content after button text. Typically used for icons.
 *
 * @csspart base - The component's base wrapper.
 * @csspart start - The container that wraps the start slot.
 * @csspart label - The button's label.
 * @csspart end - The container that wraps the end slot.
 */
declare const buttonBaseClass: import("../../../internal/face/mixin.js").MixinReturn<typeof MadeElement, import("../../../internal/face/element-internals.js").WithElementInternals>;
export default class MadeButton extends buttonBaseClass implements FormSubmitter {
    static styles: CSSResultGroup;
    static readonly formAssociated = true;
    static dependencies: {
        'made-loader': typeof MadeLoader;
    };
    private readonly hasSlotController;
    button: HTMLButtonElement | HTMLLinkElement;
    title: string;
    /** The button's theme variant. */
    variant: 'primary' | 'secondary' | 'destructive' | 'ghost';
    /** The button's size. */
    size: 'small' | 'medium' | 'large';
    /** Disables the button. */
    disabled: boolean;
    /**
     * The type of button. Note that the default value is `button` instead of `submit`, which is opposite of how native
     * `<button>` elements behave. When the type is `submit`, the button will submit the surrounding form.
     */
    type: 'button' | 'submit' | 'reset';
    /**
     * The name of the button, submitted as a name/value pair with form data, but only when this button is the submitter.
     * This attribute is ignored when `href` is present.
     */
    name: string;
    /**
     * The value of the button, submitted as a pair with the button's name as part of the form data, but only when this
     * button is the submitter. This attribute is ignored when `href` is present.
     */
    value: string;
    /** When set, the underlying button will be rendered as an `<a>` with this `href` instead of a `<button>`. */
    href: string;
    /** Tells the browser where to open the link. Only used when `href` is present. */
    target: '_blank' | '_parent' | '_self' | '_top';
    /**
     * When using `href`, this attribute will map to the underlying link's `rel` attribute. Unlike regular links, the
     * default is `noreferrer noopener` to prevent security exploits. However, if you're using `target` to point to a
     * specific tab/window, this will prevent that from working correctly. You can remove or change the default value by
     * setting the attribute to an empty string or a value of your choice, respectively.
     */
    rel: string;
    /** Tells the browser to download the linked file as this filename. Only used when `href` is present. */
    download?: string | boolean;
    /** Controls whether the button is in loading state. */
    loading: boolean;
    /**
     * If true, the button will take up the full width of its container.
     */
    fullWidth: boolean;
    /**
     * The associated form element with which this element's value will submit.
     */
    get form(): HTMLFormElement | null;
    private isLink;
    /** Simulates a click on the button. */
    click(): void;
    /** Sets focus on the button. */
    focus(options?: object): void;
    /** Removes focus from the button. */
    blur(): void;
    render(): import("lit-html").TemplateResult;
}
export {};
//# sourceMappingURL=button.component.d.ts.map