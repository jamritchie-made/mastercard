import MadeElement from '../../../internal/made-element.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary A Tab Panel is used inside a Tabs component.
 * A Tab Panel displays the content associated with a given Tab.
 * @documentation '../stories/tab-panel.mdx'
 *
 * @slot - The tab panel's content.
 *
 * @csspart base - The component's base wrapper.
 *
 * @cssproperty --padding - The tab-panels padding.
 */
export default class MadeTabPanel extends MadeElement {
    static styles: CSSResultGroup;
    private readonly tabPanelId;
    private readonly elementId;
    /** The tab panel's name. */
    name: string;
    /**
     * @internal
     *  When true, the tab panel will be shown. */
    active: boolean;
    connectedCallback(): void;
    protected updated(changedProperties: Map<string | number | symbol, unknown>): void;
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=tab-panel.component.d.ts.map