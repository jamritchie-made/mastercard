import { type CSSResultGroup, type PropertyValues } from 'lit';
import MadeElement from '../../../internal/made-element.js';
/**
 * @summary Tooltips display contextual information about an element when it receives focus or is hovered over.
 * @documentation '../stories/tooltip.mdx'
 *
 * @fires mde-show - Emitted when the tooltip opens.
 * @fires mde-hide - Emitted when the tooltip closes.
 *
 * @slot - The anchor element that triggers the tooltip.
 *
 * @csspart tooltip - The tooltip's popover container.
 * @csspart header - The tooltip header text.
 * @csspart content - The tooltip body text.
 *
 * @cssproperty --max-width - Controls the maximum width of the tooltip. Default: `18rem`.
 */
type Position = 'top' | 'bottom' | 'left' | 'right';
export default class MadeTooltip extends MadeElement {
    static styles: CSSResultGroup;
    private static _idCounter;
    tooltipPopover: HTMLElement;
    slotEl: HTMLSlotElement;
    private anchor;
    private calculatedPosition;
    private anchorHasFocus;
    private clickedViaPointer;
    private hoverTimeout;
    private _pointerMoveFrame;
    private supportsAnchor;
    /** Tooltip header */
    header: string;
    /** Tooltip content */
    content: string;
    /** Position of the tooltip in relation to its anchor: top, bottom, left, right */
    position: Position;
    /** Delay in ms before showing the tooltip on hover */
    delayDuration: number;
    /** Whether to show a pointer (beak) on the tooltip */
    pointer: boolean;
    /** Whether the tooltip is currently visible */
    open: boolean;
    /** The interaction(s) that trigger the tooltip. Space-separated list of: hover, focus, click */
    trigger: string;
    /** Disables the tooltip so it will not show */
    disabled: boolean;
    /** Appearance attribute inverts the tooltip's background and text colors */
    appearance: 'default' | 'inverted';
    firstUpdated(): void;
    updated(changedProperties: PropertyValues): void;
    disconnectedCallback(): void;
    private hasTrigger;
    private _updatePosition;
    private _handleScrollResize;
    private _detectActualPosition;
    private _animateClose;
    private _positionFallback;
    private _doShow;
    private _doHide;
    /** Shows the tooltip */
    show(): void;
    /** Hides the tooltip */
    hide(): void;
    private handleMouseOver;
    private handleMouseOut;
    private handleTouchTap;
    private handleMouseDown;
    private handleDocumentPointerMove;
    private handleDocumentKeyDown;
    private handleWindowBlur;
    private handlePointerUp;
    private handleFocus;
    private handleBlur;
    render(): import("lit-html").TemplateResult<1>;
}
export {};
//# sourceMappingURL=tooltip.component.d.ts.map