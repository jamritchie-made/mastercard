import { CheckboxValidator } from '../../../internal/face/validators/checkbox-validator.js';
import { createValidator, getValidityAnchor } from '../../../internal/face/constraint-validation.js';
import { getFormState, getFormValue } from '../../../internal/face/form-associated.js';
import { onReportValidity } from '../../../internal/face/on-report-validity.js';
import MadeElement from '../../../internal/made-element.js';
import MadeIcon from '../../icon/src/icon.component.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary The Checkbox gives users a clear way to make selections, such as agreeing to
 * terms or choosing multiple items from a list.
 * @documentation '../stories/checkbox.mdx'
 *
 * @dependency made-icon
 *
 * @fires change {Event} - Fired when an alteration to the control's value is committed by the user.
 * @fires input {Event} - Fired when the control receives input.
 * @fires invalid {Event} - Fired when the control has been checked for validity and doesn't satisfy its constraints.
 *
 * @slot - The checkbox's label. Alternatively, you can use the `label` attribute.
 * @slot supporting-text - Text that describes how to use the checkbox. Alternatively, you can use the `supporting-text` attribute.
 *
 * @csspart form-control - The outer wrapper.
 * @csspart input-container - The container that wraps the control, label and supporting text.
 * @csspart control - Checkbox control.
 * @csspart checked-icon - The checked icon element.
 * @csspart checked-path - The svg path of the checked icon.
 * @csspart indeterminate-icon - The indeterminate icon element.
 * @csspart label - The label's wrapper.
 * @csspart supporting-text - The supporting text's wrapper.
 */
declare const checkboxBaseClass: import("../../../internal/face/mixin.js").MixinReturn<import("../../../internal/face/mixin.js").MixinReturn<import("../../../internal/face/mixin.js").MixinReturn<(abstract new (...args: any[]) => import("../../../internal/face/element-internals.js").WithElementInternals) & typeof MadeElement & import("../../../internal/face/form-associated.js").FormAssociatedConstructor, import("../../../internal/face/form-associated.js").FormAssociated>, import("../../../internal/face/constraint-validation.js").ConstraintValidation>, import("../../../internal/face/on-report-validity.js").OnReportValidity>;
export default class MadeCheckbox extends checkboxBaseClass {
    static styles: CSSResultGroup;
    static dependencies: {
        'made-icon': typeof MadeIcon;
    };
    private readonly hasSlotController;
    checkbox: HTMLInputElement;
    /** Constraint validation error state controlled by `reportValidity()` */
    private hasNativeError;
    /**
     * The validation message displayed from a native error via
     * `reportValidity()`.
     */
    private nativeErrorText;
    /** Disables the checkbox. */
    disabled: boolean;
    /** The name of the checkbox, submitted as a name/value pair with form data. */
    name: string;
    /** The checkbox's label. If you need to display HTML, use the `label` slot instead. */
    label: string;
    /** The checkbox's supporting text. If you need to display HTML, use the `supporting-text` slot instead. */
    supportingText: string;
    /** The current value of the checkbox, submitted as a name/value pair with form data. */
    value: string;
    /** The checkbox's size. */
    size: 'medium' | 'large';
    /** Toggle the checkbox. */
    checked: boolean;
    /** Set the checkbox to indeterminate state. */
    indeterminate: boolean;
    /** Makes the checkbox readonly. */
    readonly: boolean;
    /** Makes the checkbox a required field. */
    required: boolean;
    /** Use the appearance attribute to change the visual style of the checkbox. */
    appearance: 'neutral' | 'branded';
    /**
     * Gets or sets whether or not the text field is in an invalid state.
     *
     * This error state overrides the error state controlled by
     * `reportValidity()`.
     */
    error: boolean;
    /**
     * The error message that replaces supporting text when `error` is true. If
     * `errorText` is an empty string, then the supporting text will continue to
     * show.
     *
     * This error message overrides the error message displayed by
     * `reportValidity()`.
     */
    errorText: string;
    private get hasError();
    private handleChange;
    private handleInput;
    /** Simulates a click on the checkbox. */
    click(): void;
    /** Sets focus on the checkbox. */
    focus(options?: FocusOptions): void;
    /** Removes focus from the checkbox. */
    blur(): void;
    private get validationText();
    private get supportingOrValidationText();
    private renderSupportingText;
    render(): import("lit-html").TemplateResult<1>;
    /**
     * Reset the text field to its default value.
     */
    private reset;
    private getErrorText;
    [getFormValue](): string | null;
    [getFormState](): string;
    formResetCallback(): void;
    formStateRestoreCallback(state: string): void;
    [createValidator](): CheckboxValidator;
    [getValidityAnchor](): HTMLInputElement;
    [onReportValidity](invalidEvent: Event | null): void;
}
export {};
//# sourceMappingURL=checkbox.component.d.ts.map