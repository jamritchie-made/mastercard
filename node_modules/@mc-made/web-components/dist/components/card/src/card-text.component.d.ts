import MadeElement from '../../../internal/made-element.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary Cards can be used to group related subjects in a container.
 * @documentation TODO
 *
 * @csspart base - The component's base wrapper.
 * @csspart title-container - Title and eyebrow container.
 * @csspart eyebrow - Eyebrow container.
 * @csspart title - Title container.
 * @csspart paragraph - Paragraph container.
 */
export default class MadeCardHeader extends MadeElement {
    static styles: CSSResultGroup;
    /** Provide context or supplementary information. */
    eyebrow: string;
    /** Provide headline text. */
    title: string;
    /** Provide body copy. */
    paragraph: string;
    /** Place the `title` above the `eyebrow`. */
    positionTitleTop: boolean;
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=card-text.component.d.ts.map