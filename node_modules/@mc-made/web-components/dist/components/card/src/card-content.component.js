/* v8 ignore next 6 */
import { html } from 'lit';
import componentStyles from '../../../styles/component.styles.js';
import MadeElement from '../../../internal/made-element.js';
import styles from './card-content.styles.js';
/**
 * @summary The wrapper for the card content
 * @documentation TODO
 *
 * @slot - Card custom content.
 *
 * @csspart base - The container that wraps custom content.

 */
class MadeCardContent extends MadeElement {
    render() {
        return html `
      <div part="base" class="made-c-card__content">
        <slot></slot>
      </div>
    `;
    }
}
MadeCardContent.styles = [componentStyles, styles];
export default MadeCardContent;
