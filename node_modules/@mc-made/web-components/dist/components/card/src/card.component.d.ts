import MadeElement from '../../../internal/made-element.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary Cards can be used to group related information into flexible containers.
 * @documentation '../stories/card.mdx'
 *
 * @slot - The card's content.
 *
 * @csspart base - The component's base wrapper.
 *
 */
export default class MadeCard extends MadeElement {
    static styles: CSSResultGroup;
    constructor();
    button: HTMLButtonElement | HTMLLinkElement | null;
    title: string;
    /** Make the whole card a primary action area. */
    clickable: boolean;
    /** Give the card a horizontal layout. */
    horizontal: boolean;
    /** Disables a clickable card. */
    disabled: boolean;
    /**
     * When set, a 'clickable' card will act as a link with this href property.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/a#href
     */
    href: string;
    /**
     * Tells the browser where to open the link. Only used when `href` is present.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/a#target
     */
    target: '_blank' | '_parent' | '_self' | '_top';
    /**
     * When using `href`, this attribute will map to the underlying link's `rel` attribute. Unlike regular links, the
     * default is `noreferrer noopener` to prevent security exploits. However, if you're using `target` to point to a
     * specific tab/window, this will prevent that from working correctly. You can remove or change the default value by
     * setting the attribute to an empty string or a value of your choice, respectively.
     *
     * https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/rel
     */
    rel: string;
    /** An accessible label that describes what happens when the component is clicked. */
    ariaLabel: string;
    private isLink;
    /** Simulates a click on the button. */
    click(): void;
    /** Sets focus on the button. */
    focus(options?: FocusOptions): void;
    /** Removes focus from the button. */
    blur(): void;
    private get touchTarget();
    render(): import("lit-html").TemplateResult;
}
//# sourceMappingURL=card.component.d.ts.map