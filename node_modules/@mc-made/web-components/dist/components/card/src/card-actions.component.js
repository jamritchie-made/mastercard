/* v8 ignore next 6 */
import { html } from 'lit';
import componentStyles from '../../../styles/component.styles.js';
import MadeElement from '../../../internal/made-element.js';
import styles from './card-actions.styles.js';
/**
 * @summary Cards can be used to group related subjects in a container.
 * @documentation TODO
 *
 * @slot - Card actions content.
 *
 * @csspart base - The component's base wrapper.
 *
 */
class MadeCardActions extends MadeElement {
    render() {
        return html `
      <div part="base" class="made-c-card__actions">
        <slot></slot>
      </div>
    `;
    }
}
MadeCardActions.styles = [componentStyles, styles];
export default MadeCardActions;
