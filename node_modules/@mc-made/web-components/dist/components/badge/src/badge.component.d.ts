import MadeElement from '../../../internal/made-element.js';
import type { CSSResultGroup } from 'lit';
/**
 * @summary A badge is a visual indicator that communicates a status or description of an associated component.
 * @documentation '../stories/badge.mdx'
 *
 * @slot - The default slot.
 *
 * @csspart base - The component's base wrapper.
 */
export type VerticalPosition = 'top' | 'bottom';
export type HorizontalPosition = 'left' | 'right';
export type Sentiment = 'neutral' | 'success' | 'danger' | 'warning' | 'information' | 'brand';
export type Variant = 'status' | 'number' | 'text';
export type Size = 'medium' | 'large';
export type Contrast = 'high' | 'low';
export default class MadeBadge extends MadeElement {
    static styles: CSSResultGroup;
    private readonly hasSlotContent;
    /** Text or number displayed inside the badge. */
    content: string;
    /** The badge's sentiment defines it's color. */
    sentiment: Sentiment;
    /** Type of content or status dot.  */
    variant: Variant;
    /** Size of the badge. */
    size: Size;
    /** Alter the contrast of the sentiment attribute. */
    contrast: Contrast;
    /** Control the visibility of the Badge. */
    invisible: boolean;
    /** When the badge component wraps an associated component, control which corner it's positioned at. */
    anchorOrigin: {
        vertical: VerticalPosition;
        horizontal: HorizontalPosition;
    };
    /** Set to true when the badge component wraps an associated component thats circular for better positioning.  */
    circularOverlap: boolean;
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=badge.component.d.ts.map