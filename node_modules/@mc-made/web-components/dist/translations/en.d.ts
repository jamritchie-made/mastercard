import type { Translation } from '../utilities/localize.js';
/**
 * English (default) translation for Made Web Components.
 *
 * This is the fallback translation used when no matching locale is found.
 * All other translation files should mirror the keys defined here.
 *
 * To add support for a new language, copy this file, rename it to the BCP 47
 * language tag (e.g. `fr.ts`, `de.ts`, `zh-cn.ts`), translate each value,
 * and call `registerTranslation(translation)` at the end.
 *
 * @example Registering a translation in your app:
 * ```js
 * import '@mc-made/web-components/translations/fr.js';
 * document.documentElement.lang = 'fr';
 * ```
 */
declare const translation: Translation;
export default translation;
//# sourceMappingURL=en.d.ts.map